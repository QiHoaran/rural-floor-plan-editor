# 发现与决策

## 2026-08-14 本轮实现约束
- 主页卡片网格、排序、选择与批处理保持不变，只调整卡片内部。
- 缩略图优先级为墙体矢量、参考图、空白；项目摘要扩展仅是 API 派生数据。
- 默认视图按 CSS 96 DPI 的 1:150 计算，viewport 不持久化；参考图首次适配由 80% 改为 10%。
- 模板三个输入映射到既有 `main_room_bay_mm`、`main_room_width_mm`、`bay_count`。
- 参考图删除在项目锁内移动文件并原子更新草稿，写入失败时回滚文件移动。
- 项目预览由服务端按当前文档即时返回，不创建缩略图文件或持久化字段。
- 同一项目内部刷新保留当前 viewport；加载不同项目时重置为 1:150，避免跨项目继承缩放。

## 需求
- 本轮按顺序优化参考图显示、撤销重做与自动保存、外墙复制、墙上构件吸附、构件端距和建筑草图模板。
- 不允许新增、删除或重命名 BuildingDocument/JSON Schema 字段。
- 构件分点采用 1/4、1/2、3/4；允许零端距。
- 建筑模板由用户在编辑器手动应用，默认 10 m × 4.5 m、4 个等宽房间，仅生成墙体；非空项目确认后清空并替换几何。
- 支持单条录入，枚举字段逐个选择。
- 支持批量导入用户给出的表格型调查数据。
- 将属性数据纳入 JSON 持久化。
- 在属性页面展示并允许更改。
- 字段包括村号、户号、性别、年龄、家庭常驻人口数、人口结构、家庭年收入、主要收入来源、房屋建造年代、建筑面积、建筑净高、平面形式、建筑结构、正房/厢房尺寸及开间数。
- 兼容新数据中房屋建造年代编码 `9`；15 条记录中恰有 8 条使用该编码，与界面报告的 8 个问题一致。
- 批量属性导入生成的无参考图项目，进入属性页后应能追加参考图。
- 建造年代采用完整编码：0 不确定；1–6 为 1950、1960、1970、1980、1990、2000 年及以后；7 为 1940 年代；8 为 1930 年代；9 为 1920 年代及以前。

## 研究发现
- `EditorLayout` 与 `SvgCanvas` 同时监听 Ctrl+Z/Ctrl+Shift+Z，可能导致一次按键执行两次历史操作。
- 自动保存成功回调当前直接用服务器文档替换内存文档，保存期间发生的撤销/重做或编辑可能被旧响应覆盖；请求因新编辑中止时，服务端仍可能已经推进 revision。
- 参考图初始 `scale` 为 1，画布 viewport 固定为 0.1 px/mm，尚无按图片边界首次适配逻辑。
- 单段墙提交后已有 `continuationAnchor`，可在不持久化新字段的前提下扩展本地命令状态以记录上一段向量。
- 墙上构件当前按鼠标投影直接放置，且命令与拓扑校验共同强制 100 mm 端距。
- 现有 `insertWall` 与 `recomputeGeometry` 可复用来生成模板，并通过一次 Zustand `transact` 形成单步撤销。
- 墙上构件拖拽在 `WallElementLayer` 内再次硬编码 100 mm 端距；尺寸编辑位于 `ConnectivityPanel`，需要与放置命令共享同一合法区间逻辑。
- 保存成功后必须同时重定位 undo/redo 历史快照的 revision，否则“先保存、再撤销”仍会用旧 revision 触发冲突。
- 建筑模板顺序插入外框和分隔墙后，现有 normalizeGraph 会自动拆分上下外墙，recomputeGeometry 可直接形成等分房间面。
- 项目为 React 19 + TypeScript + Zustand，前端通过 Express API 保存完整 `BuildingDocument` JSON。
- 正式领域模型版本为 `2.1.0`，已有 `metadata.village_code` 与 `metadata.household_code`，但没有住户调查属性对象。
- 右侧 `EditablePropertyPanel` 当前仅在选中墙、面、构件、顶点或调整参考图时显示对应属性；无选择时是空提示，适合改为建筑/住户属性编辑入口。
- `transact` 会记录撤销历史、标记未保存，随后现有自动保存流程可将新增属性写入建筑 JSON。
- JSON Schema 会校验保存文档，因此领域类型、默认文档和 schema 必须同步扩展。
- 新建项目目前要求建筑 ID、参考草图和默认墙厚；批量导入更适合在编辑器属性页针对当前建筑粘贴 TSV/CSV/JSON，并用 rural + house 匹配当前记录，避免批量创建缺少参考草图的项目。
- 服务端当前创建接口强制要求参考图，但 `BuildingDocument.reference_image` 本身允许空路径和 0 尺寸，因此可以安全支持“仅属性”的批量项目，后续再补参考图/绘图。
- 建筑 ID 只能使用字母、数字、下划线和短横线；批量记录可稳定生成 `rural_{村号}_house_{户号}` ID。
- 首页已有新建、列表、删除和恢复，可增加独立批量导入对话框；导入后刷新项目列表。
- 每个项目的最新草稿位于 `data/{buildingId}/draft/building.autosave.json`；列表和打开逻辑优先读取较新的草稿。
- 服务端 `autosave` 已执行 schema 校验并维护 revision，批量更新既有项目应复用同等字段更新规则；新建数据项目需创建标准子目录和初始草稿 JSON。
- 根节点全局设置 `overflow: hidden`；主页面仅使用 `min-height: 100%`，内容会撑高元素而不会形成内部滚动区，因此滚轮无法显示下方内容。
- 编辑器的 flex 子节点 `.body`、`.rightPanel`、`.panelContent` 缺少 `min-height: 0`，属性面板也没有继承可滚动高度，长表单会被裁切。
- 核心几何字段（顶点、墙、门窗、参考图变换、标定）均已使用 `_mm`，输入端通过 `parseMeters` 乘以 1000；新增调查数据中的净高和正房/厢房尺寸仍以 `_m` 保存，是当前唯一违反统一毫米存储的领域字段。
- 墙高同时存在于 `survey.clear_height_mm`、`building_defaults.wall_height_mm` 和每堵墙的 `height_mm`；墙体属性面板仍允许单墙编辑，存在数据分叉风险。
- 首页卡片当前点击即打开，没有选择状态；删除和导出均已有可靠的单项目接口，批处理可在客户端逐项执行并报告部分失败。
- `offset_from_start_mm` 是门窗在宿主墙上的必要定位数据，`sill_height_mm` 也属于既有 Schema；两者应仅从属性界面隐藏，不从 JSON 删除。
- 现有房间字典包含 16 个内置类型，单建筑 `custom_function_types` 已能保存类型快照，但缺少跨建筑共享设置。
- 结构化校验函数已支持参数和类别，但中文消息尚不插值；自动保存也未刷新 `structured_validation` 与 `statistics`，导致首页问题计数可能滞后。
- `data/` 列表会忽略点号开头目录，适合用 `data/.settings/room-function-templates.json` 保存应用级模板而不会被识别为建筑。
- 项目目录打开前还需校验 `realpath` 边界，避免目录链接绕过词法路径检查；最终实现同时检查逻辑路径与真实路径。

## 技术决策
| 决策 | 理由 |
|------|------|
| 枚举字段内部保留数字编码，同时由 UI 显示中文标签 | 兼容现有数据且便于用户理解和编辑 |
| 新增独立 `survey` 属性对象而不是把所有字段塞入 metadata | 保持项目元数据与调查数据职责清晰，JSON 结构稳定 |
| 默认在未选择几何实体时展示住户属性编辑器 | 不破坏墙体/房间等既有编辑交互 |
| 批量导入在首页一次创建/更新多户的 JSON；无草图记录创建为数据项目 | 真正满足批量处理，且后续仍可逐户进入属性页修改 |
| 重复 rural + house 采用更新调查属性、保留现有几何和参考图 | 支持修正/补充调查数据且不损坏绘图成果 |
| 项目 ID 统一为 `rural_001_house_0001` | 村号补足 3 位、户号补足 4 位 |
| 房屋建造年代编码 `9` 显示为“年代不详” | 原编码表仅定义 1–6，新数据明确使用 9 作为缺失/未知类编码；保留原始编码避免篡改源数据 |
| JSON 枚举字段改为直接保存中文标签 | 按用户要求提高 JSON 可读性；输入端继续接受原数字编码，旧项目打开时自动迁移 |
| 参考图仅允许为空的属性项目追加，不在此入口覆盖已有图片 | 解决补图需求，同时避免无意破坏现有标定和绘图依据 |
| 旧值“年代不详”迁移为“1920 年代及以前” | 该旧值全部由之前的编码 9 生成，新字典明确编码 9 的新含义；编码 0 从现在起表示“不确定” |
| 调查尺寸字段改为 `_mm` 整数，米只作为输入/展示单位 | 与 BuildingDocument 的 `storage_unit: mm` 保持一致，避免不同模块重复猜测单位 |
| 调查净高更新时同步默认墙高和所有墙体高度 | 保持单一编辑入口且不破坏现有 JSON/导出契约 |
| 文件夹打开接口仅允许回环请求并使用无 shell 的平台命令 | 限制本地副作用并避免命令注入 |
| 全局模板分配后复制进建筑自定义类型快照 | 删除或修改全局模板后，历史建筑仍能稳定显示 |
| 开间检查在已有墙体时比较 `survey.bay_count` 与 `Object.keys(faces).length` | 排除未开始绘图的纯属性项目，并遵循用户确认的严格相等口径 |
| 门窗宽高通过一个解析器原子提交，Offset/Sill 仅隐藏 | 保证宽高校验和撤销语义一致，同时维持定位、碰撞检查及旧 JSON 兼容 |
| 自动保存采用单飞请求加待保存版本队列 | 避免旧响应覆盖新编辑，并保持服务器 revision 顺序一致 |
| 参考图只通过 viewport 自动适配放大 | 不修改参考图 transform 或任何持久化 JSON 值 |
| 墙上构件分点吸附只折算为 `offset_from_start_mm` | 支持 1/4、1/2、3/4 吸附而不增加持久化字段 |
| 建筑模板通过现有墙体插入与几何重算生成 | 整体可一次撤销，并保持 BuildingDocument 与 Schema 不变 |
| 门窗端距校验统一为零端距夹紧 | 2 m 墙可合法放置 1.8 m 构件，超长与重叠仍会被拒绝 |

## 遇到的问题
| 问题 | 解决方案 |
|------|---------|

## 资源
### 2026-09-07 正交检查
- Embodied_V2 的墙和房间边界要求精确水平/垂直，不允许 1 mm 偏差；不执行近轴修复或整体旋转。
- 墙体移动复用 moveVertex、normalizeGraph、recomputeGeometry；预览文档不写入编辑事务，确认后一次提交。
- 索引 readDocument 按文档版本与时间选择正式/草稿文件，测试必须使用真实保存路径及版本更新。
- normalizeGraph 默认 1 mm 合并容差，因此正交修复必须验证规范化后仍精确轴对齐，不能只相信投影目标。
- SVG 水平/垂直 line 的包围盒有零维度，Playwright 单线 toBeVisible 不适合；用预览组可见性、坐标和截图验证。

- 用户消息中的字段编码与两条样例数据。

## 视觉/浏览器发现
- 浏览器运行时因本机用户目录权限（EPERM）未能启动，无法完成截图式视觉检查；组件交互通过 jsdom 自动化测试验证。

---
*每执行2次查看/浏览器/搜索操作后更新此文件*
