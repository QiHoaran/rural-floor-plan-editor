# 任务计划：乡村建筑编辑器与住户数据优化

## 目标
在保留住户属性录入、批量导入和参考图能力的基础上，完成高度统一、首页批处理、本地文件夹、拓扑检查、寒冷地区房间模板和门窗属性简化。

## 当前阶段
已完成

## 各阶段

### 阶段 1：需求与现状发现
- [x] 理解用户提供的字段和编码
- [x] 定位现有导入、数据模型、属性页面和持久化实现
- [x] 将发现记录到 findings.md
- **状态：** complete

### 阶段 2：方案与数据结构
- [x] 定义字段、枚举、校验和兼容策略
- [x] 确定单条录入与批量导入交互
- **状态：** complete

### 阶段 3：实现
- [x] 实现批量导入与 JSON 数据记录
- [x] 实现属性页面展示和编辑
- [x] 保留或完善单条选择录入
- **状态：** complete

### 阶段 4：测试与验证
- [x] 运行静态检查和自动化测试
- [x] 验证示例数据导入、展示、修改和保存
- [x] 修复发现的问题
- **状态：** complete

### 阶段 5：交付
- [x] 检查变更范围和输出文件
- [x] 总结使用方法与验证结果
- **状态：** complete

### 阶段 6：兼容扩展编码
- [x] 将房屋建造年代编码 9 定义为“年代不详”
- [x] 同步类型、下拉选项、解析校验和 JSON Schema
- [x] 使用用户提供的 15 条数据验证无错误导入
- **状态：** complete

### 阶段 7：页面滚动与选项显示
- [x] 修复主页面内部滚动高度约束
- [x] 修复编辑器属性面板滚轮滚动
- [x] 去掉属性下拉项的数字前缀并验证
- **状态：** complete

### 阶段 8：JSON 枚举中文化
- [x] 将枚举属性的持久化值由数字改为中文
- [x] 继续兼容数字编码批量导入
- [x] 打开旧项目时自动迁移数字枚举
- [x] 更新 Schema、界面和测试
- **状态：** complete

### 阶段 9：属性项目补图与年代字典
- [x] 为仅属性项目提供后续上传参考图能力
- [x] 将建造年代字典扩展为 0–9 新定义
- [x] 迁移旧年代中文值并更新 Schema
- [x] 添加服务端、组件和导入测试
- **状态：** complete

### 阶段 10：调查尺寸统一毫米存储
- [x] 将 5 个调查尺寸字段改为 `_mm`
- [x] 米制导入和界面编辑时转换为整数毫米
- [x] 旧 `_m` JSON 自动迁移为 `_mm`
- [x] 更新 Schema、文档和测试
- **状态：** complete

### 阶段 11：高度一致性与拓扑检查
- [x] 以调查净高同步默认墙高和全部墙体高度
- [x] 增加开间数与室内面数量不一致警告
- [x] 自动保存时刷新校验与统计
- **状态：** complete

### 阶段 12：首页批处理与本地文件夹
- [x] 增加建筑多选、全选、批量删除和批量导出
- [x] 增加安全的本地项目文件夹打开接口与编辑器入口
- **状态：** complete

### 阶段 13：寒冷地区房间模板
- [x] 将内置功能缩减为卧室、客厅、餐厅
- [x] 增加应用级自定义模板 CRUD 与兼容快照
- **状态：** complete

### 阶段 14：门窗属性简化
- [x] 隐藏 Offset 与 Sill 编辑字段
- [x] 将尺寸改为宽×高联合输入与校验
- **状态：** complete

### 阶段 15：测试与交付
- [x] 补充单元、组件和服务端测试
- [x] 运行 build、lint、全量测试与 diff 检查
- [x] 检查未提交变更边界并更新文档
- **状态：** complete

## 关键问题
1. 当前应用采用何种技术栈和状态持久化方式？
2. rural + house 是否共同构成住户/房屋数据的稳定标识？

## 已做决策
| 决策 | 理由 |
|------|------|
| 先兼容用户提供的数字编码 | 便于直接批量导入现有调查数据 |
| 以 rural + house 作为批量数据自然键，生成稳定项目 ID | 与用户表格结构一致并避免跨村户号冲突 |
| 项目 ID 统一为 `rural_001_house_0001` | 按用户追加要求，村号 3 位、户号 4 位补零 |
| 净高为唯一可编辑高度，墙高仅作兼容同步字段 | 简化界面同时保持现有 Schema、导出和旧数据兼容 |
| 首页多选支持批量回收站删除和逐栋 ZIP 导出 | 满足批处理需求且复用现有可恢复删除与导出接口 |
| 房间内置模板仅保留卧室、客厅、餐厅，自定义模板全项目复用 | 面向寒冷地区乡村住宅，同时保留历史标注快照 |
| 开间数与室内面总数不等即给出警告 | 按用户确认的严格检查口径实现 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 首次批量更新计划时补丁上下文顺序不匹配 | 1 | 读取当前规划文件后按实际上下文拆分更新 |
| 追加 ID 格式时补丁包含不存在的 findings 上下文 | 1 | 定位实际行后分别更新 |
| TypeScript TS2783：列表字段对象中的 key 与 React key 重名 | 1 | 显式传入组件属性，不展开含 key 的对象 |
| 浏览器运行时启动失败：用户目录 EPERM | 1 | 保留为已知验证限制，使用组件/API/全量测试覆盖交互 |
| 收尾更新计划时补丁上下文顺序不匹配 | 1 | 读取计划后按实际位置更新 |
| 无权通过 CIM 查询本地验证服务进程 | 1 | 用 netstat 精确定位本任务启动的 4173 端口进程并停止 |
| 临时日志被验证服务锁定，首次删除失败 | 1 | 按安全规则停止删除重试；日志为 gitignore 文件，不影响交付 |
| TSV 表头过早映射成 `_mm`，导致米值被误判为已是毫米 | 1 | 保留原始表头到规范化阶段，再按表头单位决定是否乘以 1000 |
| 首页批处理综合补丁的 CSS 上下文未匹配 | 1 | 按实际文件位置拆分为 API、组件和样式三个补丁 |

## 备注
- 不覆盖用户已有的无关修改。
