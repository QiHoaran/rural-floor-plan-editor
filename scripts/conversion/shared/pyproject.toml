[project]
name = "conversion-shared"
version = "1.0.0"
description = "Deterministic cleaner and shared conversion framework for rural floor plans"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
conversion-clean = "conversion_shared.cli:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
